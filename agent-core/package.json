{
  "name": "agent-core",
  "version": "0.1.0",
  "private": true,
  "description": "V3 headless agent runtime: AgentLoop + memory + skill tool-call. No shell, no CLI, no apply_patch.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "main": "dist/worker.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "dotenv": "^16.4.5",
    "openai": "^4.89.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
